{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "expect": {
      "type": "local",
      "command": [
        "bunx",
        "-y",
        "-a",
        "gemini",
        "expect-cli@latest",
        "mcp"
      ],
      "enabled": true
    }
  }
}